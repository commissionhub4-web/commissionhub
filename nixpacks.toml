[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd CommissionHub_backend && python -m pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "cd CommissionHub_backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
